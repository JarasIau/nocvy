NOCZWY
Noczwy is a tiny python program, that enumerates directories of a targeted website and writes its output to stdout.

USAGE
NOCZWY takes arguments, some of what you have to specify before executing the program.
-h, --help : show the help message and exit.
-u, --uri : save uri to begin the enumeration process.
-w, --wordlist : point NOCZWY to a wordlist.
-m, --head : use HEAD method instead of GET.
-t, --threads : a number of threads to be used.
  
NOCZWY does not automatically filter output. You can do it yourself by piping it into some other program:
python3 src/noczwy.py -u https://<website>/ -w src/wl.txt | grep 200
OR
python3 src/noczwy.py -u https://<website>/ -w src/wl.txt | grep -v 404
